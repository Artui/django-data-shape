# django-data-shape

A realistically shaped test database from Django models.

Declare cardinality, value skew and foreign-key fan-out as a distribution, load
by `COPY`, and `ANALYZE` so the query planner sees the shape rather than a
row count.

This documentation is a stub; it grows with the API in 0.1.0.
